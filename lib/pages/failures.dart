import 'package:flutter/material.dart';

class Failures extends StatelessWidget {
  const Failures({super.key});

  @override
  Widget build(BuildContext context) {
    return const Text("Systemausfälle");
  }
}
