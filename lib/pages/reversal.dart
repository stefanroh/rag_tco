import 'package:flutter/material.dart';

class Reversal extends StatelessWidget {
  const Reversal({super.key});

  @override
  Widget build(BuildContext context) {
    return const Text("Rückabwicklung");
  }
}
